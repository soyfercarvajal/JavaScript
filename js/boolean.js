//Tipo de dato Boolean

let verdadero = true;
let falso = false;

console.log(typeof verdadero, typeof falso);

//Valores verdaderos en JavaScript
/*  
    if (true)
    if ([])
    if (42)
    if ("foo")
    if ({})
    if (new Date())
    if (-42)
    if (3.14)
    if (-3.14)
    if (Infinity)
    if (-Infinity)
*/
